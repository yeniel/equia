name: data
description: Data layer
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  ###########################
  # MODULES

  domain:
    path: ../domain

  ###########################
  # PLUGINS AND PACKAGES

  http: any
  json_annotation: any
  json_serializable: any
  equatable: any
  path: any
  sqflite: any
  google_sign_in: any
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  firebase_crashlytics: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: any
  build_runner: any

flutter:
